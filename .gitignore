# ============================================================================
# SysScanGUI
# 原则：只把「源码 + 测试 + 构建配置」纳入版本库；
#      一切扫描产物、构建产物、备份与本地工作记录都不入库。
# ============================================================================

# ---------------------------------------------------------------------------
# 1. 扫描产物 —— 内含主机名、账户名、完整进程命令行与远端 IP，属本机隐私数据
# ---------------------------------------------------------------------------
scan_result.json
report.html
scan_result*.json
report*.html

# ---------------------------------------------------------------------------
# 2. Python 字节码缓存
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/

# ---------------------------------------------------------------------------
# 3. 虚拟环境
# ---------------------------------------------------------------------------
.venv/
venv/
env/
ENV/

# ---------------------------------------------------------------------------
# 4. PyInstaller 构建产物（可随时用 SysScanGUI.spec 重新生成，体积 ~47 MB）
# ---------------------------------------------------------------------------
build/
dist/
*.manifest

# ---------------------------------------------------------------------------
# 5. 历史版本备份与本地调试产物
# ---------------------------------------------------------------------------
backup_*/
*_pre_*.exe
_shot_*.png
_shot_*.py
_elevate_tmp/
*.log
# --smoke 自检写出的结果文件（CI 会断言其内容，本地跑完即弃）
smoke_result.json
smoke_result*.json

# ---------------------------------------------------------------------------
# 6. WorkBuddy 本地工作记忆（含开发者本机绝对路径，不上传）
# ---------------------------------------------------------------------------
.workbuddy/

# ---------------------------------------------------------------------------
# 7. 编辑器 / 系统垃圾
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*~
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
