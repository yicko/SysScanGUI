# SysScanGUI 运行期依赖
#   pip install -r requirements.txt

# 进程 / 服务 / 网络连接 / 资源占用采集
psutil>=5.9.0

# 图形界面（Qt 6 的精简发行版，仅含 QtCore / QtGui / QtWidgets，无需完整 PySide6）
# 若已安装完整版 PySide6，此依赖同样满足。
PySide6-Essentials>=6.5.0

# ---------------------------------------------------------------------------
# 可选：仅打包可执行文件时需要（日常运行、跑测试都不需要）
#   pip install pyinstaller
# ---------------------------------------------------------------------------
